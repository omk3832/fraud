# Optional profile for extra local overrides. Default application.properties already enables DE stub
# (fraud.de.stub.enabled=true) so you can run without this profile.
#
# Alternate: Postman sends full feature JSON with fraud.de.stub.enabled=false and fraud.de.enabled=false
fraud.de.stub.enabled=true
fraud.de.enabled=true
